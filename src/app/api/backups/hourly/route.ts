//import { getStorageContext } from '@/lib/server/db'

export async function POST() {
  // const { db } = getStorageContext()
  // TODO: verify basic auth and then figure out which backups to run and run them


  return Response.json({})
}